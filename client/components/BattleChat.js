import React from 'react';

export default class BattleChat extends React.Component {

	constructor(){
    super();
  }

	render () {
		// return (
		//     <div>{this.props.messages.map((msg) => <div>{msg}</div>)}</div>
  //   )

    return(<div></div>)
	}
}